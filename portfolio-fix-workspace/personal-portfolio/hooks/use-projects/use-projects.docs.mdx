---
description: A UseProject Hook.
---

import { useProjects } from './use-projects';

A hook that fetches the projects.

### Component usage

```js
const { projects, loading } = useProjects({ type?:ProjectKind });
```
