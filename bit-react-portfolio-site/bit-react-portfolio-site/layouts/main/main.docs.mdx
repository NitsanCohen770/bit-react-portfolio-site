---
description: A Main Layout omponent.
labels: ['Layout']
---

import { Main } from './main';
import { Footer } from '@enlear/bit-react-portfolio-site-design-system.navigation.footer';
import { Header } from '@enlear/bit-react-portfolio-site-design-system.navigation.header';
import { lightTheme } from '@enlear/bit-react-portfolio-site-design-system.theme.light-theme';
import { ThemeProvider } from '@enlear/bit-react-portfolio-site-design-system.theme.theme-provider';
import { ThemeToggle } from '@enlear/bit-react-portfolio-site-design-system.theme.theme-toggle';
import { Logo } from '@showoff/design.brand.logo';
import { Typography } from '@enlear/bit-react-portfolio-site-design-system.widgets.typography';

A component that acts as a layout component and positions the header, footer and content.

### Component usage

```js
<Main header={<></>} footer={<></>}>
  {children}
</Main>
```

### Render hello world!

```js live
<ThemeProvider theme={lightTheme()}>
  <Main
    footer={
      <Footer
        rightContent="© 2022 Enlear. All Rights Reserved."
        leftContent="Made with ❤️ by Enlear"
      />
    }
    header={
      <Header
        logo={<Logo />}
        navLinks={[
          { label: 'Home', url: '/' },
          { label: 'About', url: '/about' },
          { label: 'Contact', url: '/contact' },
        ]}
        darkToggleButton={
          <ThemeToggle
            isDark={true}
            onClick={() => alert('Turn Dark Off')}
            size="small"
          />
        }
      />
    }
  >
    <Typography variant="h1">Hello World</Typography>
  </Main>
</ThemeProvider>
```
