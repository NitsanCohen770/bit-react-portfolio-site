---
description: A Header component.
---

import { Header } from './header';
import { Logo } from '@showoff/design.brand.logo';
import { ThemeToggle } from '@showoff/design.theme.theme-toggle';
import { lightTheme } from '@showoff/design.theme.light-theme';
import { ThemeProvider } from '@showoff/design.theme.theme-provider';

A component that renders a header component for an application.

### Component usage

```js
<Header
  logo={<Logo />}
  navLinks={[
    { label: 'Projects', url: '/projects' },
    { label: 'Contact', url: '/contact' },
    { label: 'About', url: '/about' },
  ]}
  themeToggle={
    <ThemeToggle
      size="small"
      isDark={isDark}
      onClick={() => setIsDark(!isDark)}
    />
  }
/>
```

### Render sample header

```js live
<ThemeProvider theme={lightTheme()}>
  <Header
    logo={<Logo />}
    navLinks={[
      { label: 'Projects', url: '/projects' },
      { label: 'Contact', url: '/contact' },
      { label: 'About', url: '/about' },
    ]}
    themeToggle={
      <ThemeToggle
        size="small"
        isDark={false}
        onClick={() => alert('Hello Dark Mode')}
      />
    }
  />
</ThemeProvider>
```
